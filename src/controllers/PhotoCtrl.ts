import * as e from 'express';
import oRest = require('../utils/restware');
/**
 * Purpose : Cotroller for photo service.
 * 
 * 1. Photo upload
 * 2. Photo search
 */
module.exports = {
    upload: function (req: e.Request, res: e.Response) {
    },
    search: function (req: e.Request, res: e.Response) {
    }
}